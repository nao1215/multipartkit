name = "multipartkit_parse_request"
version = "0.1.0"
target = "erlang"
description = "Runnable example: parse an incoming HTTP multipart/form-data request"

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
multipartkit = { path = "../.." }
