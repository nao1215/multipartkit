name = "multipartkit_streaming_parse"
version = "0.1.0"
target = "erlang"
description = "Runnable example: parse a multipart body using parse_stream"

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_yielder = ">= 1.0.0 and < 2.0.0"
multipartkit = { path = "../.." }
